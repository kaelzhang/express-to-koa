{
  "name": "express-to-koa",
  "version": "1.0.7",
  "description": "Use express middlewares in Koa2, the one that really works.",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "test": "npm run build && node ./node_modules/.bin/ava --verbose --timeout=10s",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/kaelzhang/express-to-koa.git"
  },
  "keywords": [
    "express-to-koa",
    "express",
    "koa",
    "koa2",
    "middleware",
    "converter",
    "connect"
  ],
  "engines": {
    "node": ">=4"
  },
  "author": "kaelzhang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kaelzhang/express-to-koa/issues"
  },
  "ava": {
    "require": [
      "@babel/register"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/preset-env": "^7.3.4",
    "@babel/register": "^7.0.0",
    "ava": "^1.3.1",
    "axios": "^0.18.0",
    "koa": "^2.7.0",
    "koa-router": "^7.4.0"
  }
}
